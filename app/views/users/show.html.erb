<h1>Profile</h1>

<%= @user.email %>
<center>
<% @user.posts.each do |post|%>
	<div class="row">
		<div class="col-md-12">
			
			<%= image_tag post.post_img.url(:post_show), class: "post"%>
		</div>
	</div>
<%end%>
</center>

